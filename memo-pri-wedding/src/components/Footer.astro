---
import { Icon } from 'astro-icon/components';

const currentYear = new Date().getFullYear();
const weddingDate = "20 de Diciembre, 2025";
const weddingHashtag = "#PriYMemoSeCasan";
const emailContact = "memoypri@gmail.com";
const phonePri = "+34 6 [Número Pri]"; // Replace with actual number
const phoneMemo = "+54 9 [Número Memo]"; // Replace with actual number
---

<footer class="bg-[#004ADD] text-white pt-12 pb-8">
  <div class="container mx-auto px-4 text-center max-w-2xl">

    <div class="mb-8">
      <h3 class="text-3xl font-malone-clemettine mb-2">Memo & Pri</h3>
      <p class="text-lg font-ibm-plex text-white">{weddingDate}</p>
    </div>

    <div class="mb-8">
      <a href="#" class="text-lg font-ibm-plex text-white hover:text-yellow-medium transition-colors">{weddingHashtag}</a>
    </div>

    <!-- Quick Links (optional) -->
    <!-- This entire div will be removed -->
    <!-- 
    <div class="mb-8 flex justify-center space-x-6">
      <a href="#location" class="text-white hover:text-yellow-medium transition-colors">Ubicación</a>
      <a href="#faq" class="text-white hover:text-yellow-medium transition-colors">Preguntas</a>
      <a href="#gift" class="text-white hover:text-yellow-medium transition-colors">Regalo</a>
    </div>
    -->

    <!-- Social Media Links (optional placeholder) -->
    <!-- 
    <div class="flex justify-center space-x-4 mb-8">
      <a href="#" class="text-blue-light hover:text-yellow-medium transition-colors"><i class="fab fa-instagram"></i></a> 
      <a href="#" class="text-blue-light hover:text-yellow-medium transition-colors"><i class="fab fa-facebook"></i></a>
    </div>
    -->

    <div class="border-t border-blue-medium border-opacity-30 pt-8">
      <p class="text-xs text-white opacity-50 inline-flex items-center justify-center">
        Hecha con 
        <Icon name="mdi:heart" class="inline-block h-3 w-3 mx-1 text-white" /> por los novios
      </p>
      
      <!-- Back to top button -->
      <button 
        onclick="window.scrollTo({top: 0, behavior: 'smooth'});"
        class="mt-6 text-xs text-white opacity-70 hover:opacity-100 hover:text-yellow-medium transition-all duration-300 flex items-center justify-center mx-auto group">
        Volver arriba 
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1 transform group-hover:-translate-y-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M5 15l7-7 7 7" />
        </svg>
      </button>
    </div>

  </div>
</footer>

<script>
  // Back-to-top button logic remains the same
  document.addEventListener('DOMContentLoaded', () => {
    const backToTopButton = document.getElementById('back-to-top');
    
    if (backToTopButton) {
      // Mostrar/ocultar el botón según el scroll
      window.addEventListener('scroll', () => {
        if (window.scrollY > 300) {
          backToTopButton.classList.remove('opacity-0', 'invisible');
          backToTopButton.classList.add('opacity-100', 'visible');
        } else {
          backToTopButton.classList.remove('opacity-100', 'visible');
          backToTopButton.classList.add('opacity-0', 'invisible');
        }
      });
      
      // Acción al hacer clic
      backToTopButton.addEventListener('click', () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });
    }
  });
</script>