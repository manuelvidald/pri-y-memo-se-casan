---
// Props can be added if needed later
---

<section id="gift" class="section pt-12" style="background-color: #004AAD;">
  <div class="container mx-auto px-12 text-center">
    <img src="/images/icon-regalo-white.png" alt="Icono Regalo" class="mx-auto mb-4 w-16 h-16">
    <h2 class="section-title text-white">Regalo</h2>
    
    <div class="max-w-2xl mx-auto text-center">
      
      <div class="mb-6">
        <p class="text-lg text-white mb-3 font-plex-sans font-light unified-text leading-relaxed">
          Sabemos que muchos vienen de lejos. La presencia es lo más importante.
        </p>
        <p class="text-md text-white mb-4 font-plex-sans font-light unified-text">
          Para los que preguntaron, si quieren hacernos un regalo podrían ayudarnos con el futuro hermoso que se nos viene.
        </p>
      </div>

      <!-- Opciones de Regalo -->
      <div class="space-y-4">
        <h4 class="font-semibold text-white mb-2 font-plex-sans font-light unified-text">Para los de Argentina</h4>
        <button
          class="w-48 inline-block mb-3 px-6 py-2 rounded-full bg-[#004AAD] text-white border border-white text-sm hover:bg-white hover:text-[#004AAD] transition-colors duration-300 focus:outline-none focus:ring-0 [-webkit-tap-highlight-color:transparent] active:bg-[#004AAD] active:text-white active:duration-0 font-ibm-plex-sans font-light unified-text text-center"
          data-clipboard-text="[PLACEHOLDER_CBU]"
        >
          Copiar CBU
        </button>

        <h4 class="font-semibold text-white mb-2 font-plex-sans font-light unified-text">Para los de España/Europa</h4>
        <button
          class="w-48 inline-block mb-3 px-6 py-2 rounded-full bg-[#004AAD] text-white border border-white text-sm hover:bg-white hover:text-[#004AAD] transition-colors duration-300 focus:outline-none focus:ring-0 [-webkit-tap-highlight-color:transparent] active:bg-[#004AAD] active:text-white active:duration-0 font-ibm-plex-sans font-light unified-text text-center"
          data-clipboard-text="[PLACEHOLDER_IBAN]"
        >
          Copiar IBAN
        </button>

        <p class="text-sm text-white italic mt-2">
          Hace clic para copiar los números de cuenta :)
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  // Basic structure for potential copy-to-clipboard functionality
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('[data-clipboard-text]');
    buttons.forEach(button => {
      button.addEventListener('click', () => {
        const textToCopy = button.getAttribute('data-clipboard-text');
        if (textToCopy && textToCopy.startsWith('[PLACEHOLDER_')) {
          alert('Por favor, reemplaza los placeholders con los números de cuenta reales en el código.');
          return;
        }
        navigator.clipboard.writeText(textToCopy)
          .then(() => {
            // Optional: Show a success message or change button text
            const originalText = button.textContent;
            button.textContent = '¡Copiado!';
            setTimeout(() => {
              button.textContent = originalText;
            }, 2000); // Revert back after 2 seconds
          })
          .catch(err => {
            console.error('Error al copiar al portapapeles:', err);
            alert('Hubo un error al intentar copiar el número.');
          });
      });
    });
  });
</script>